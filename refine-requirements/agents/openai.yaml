interface:
  display_name: "Refine Requirements"
  short_description: "补齐产品需求的边界、规则、异常与验收标准"
  default_prompt: "Use $refine-requirements to polish this product requirement, fill in missing boundaries, and produce review-ready acceptance criteria."

policy:
  allow_implicit_invocation: true
